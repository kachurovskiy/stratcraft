# StratCraft local configuration
# Copy this file to ".env" and fill in values.

# PostgreSQL connection string.
# Example: postgresql://postgres:132456@localhost:5432/stratcraft
DATABASE_URL=

# 32-byte encryption key (hex or base64) used to encrypt secrets stored in Postgres.
# Generate one on Linux with: openssl rand -hex 32
# On Windows use:
# $bytes = New-Object byte[] 32
# [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
# ($bytes | ForEach-Object { $_.ToString("x2") }) -join ""
DATABASE_KEY=

# Web server bind configuration.
SERVER_HOST=localhost
SERVER_PORT=3000

# Optional environment.
NODE_ENV=development

# Domain for sending emails (usually configured via Admin Settings instead).
# DOMAIN=
